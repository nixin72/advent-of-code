(ns aoc-2020.core
  (:use clojure.pprint)
  (:use aoc-2020.day1)
  (:use aoc-2020.day2)
  (:use aoc-2020.day3)
  (:use aoc-2020.day4)
  (:use aoc-2020.day5)
  (:use aoc-2020.day6)
  (:use aoc-2020.day7)
  (:use aoc-2020.day8)
  (:use aoc-2020.day9)
  (:use aoc-2020.day10))

(defn -main []
  ;; (pprint (day-1-1))
  ;; (pprint (day-1-2))
  ;; (pprint (day-2-1))
  ;; (pprint (day-2-2))
  ;; (pprint (day-3-1 1 3))
  ;; (pprint (day-4-1))
  ;; (pprint (day-4-2))
  ;; (pprint (day-5-1))
  ;; (pprint (day-5-2))
  ;; (pprint (day-6-1))
  ;; (pprint (day-6-2))
  ;; (pprint (day-7-1))
  ;; (pprint (day-7-2))
  ;; (pprint (day-8-1))
  ;; (pprint (day-8-2))
  ;; (pprint (day-9-1))
  ;; (pprint (day-9-2))
  (pprint (day-10-1))
  (pprint (day-10-2)))
