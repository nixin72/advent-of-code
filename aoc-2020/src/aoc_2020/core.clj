(ns aoc-2020.core
  (:use clojure.pprint)
  (:use aoc-2020.day1)
  (:use aoc-2020.day2)
  (:use aoc-2020.day3)
  (:use aoc-2020.day4))

(defn -main []
  ;; (pprint (day-1-1))
  ;; (pprint (day-1-2))
  ;; (pprint (day-2-1))
  ;; (pprint (day-2-2))
  ;; (pprint (day-3-1 1 3))
  (pprint (day-4-1))
  (pprint (day-4-2)))
